# ============================================================
# Root .gitignore (monorepo: frontend/ + backend/)
# Frontend punya .gitignore sendiri di frontend/.gitignore
# File ini fokus ke backend Python + hal umum.
# ============================================================

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments
.venv/
venv/
env/
ENV/
backend/.venv/
backend/venv/

# Packaging / build
*.egg-info/
.eggs/
dist/
build/
*.egg

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# --- Secrets / environment ---
# JANGAN commit kredensial DB/Redis. Hanya .env.example yang boleh di-push.
.env
.env.*
!.env.example
!**/.env.example

# --- RAG vector store (Phase 5) ---
# Turunan dari app/rag/knowledge_base.py (di-seed ulang via `python -m app.rag.knowledge_base`).
# Vektor adalah artefak biner yang dihasilkan, bukan sumber — jangan di-commit.
backend/app/rag/data/

# --- Misc ---
.DS_Store
*.log
